"use client";
import { useState } from "react";
import { Page, PanelTitle } from "../components/page-ui";
export default function ProposalPage() { const [notice, setNotice] = useState(""); return <Page title="New proposal" kicker="PROPOSAL / SOURCE REVIEW" intro="Prepare the exact source payload for review. Submission stays disabled until the real proposal contract is deployed and exposed here."><section className="panel narrow-panel"><PanelTitle label="SOURCE PAYLOAD" title="Review before signing" note="NO ACTIVE GOVERNOR"/><form className="proposal-form" onSubmit={e => { e.preventDefault(); setNotice("Proposal submission is intentionally unavailable: this checkout has no deployed proposal contract yet."); }}><div className="two-fields"><label>Proposed version<input placeholder="e.g. 2.0" required /></label><label>Immutable source URL<input placeholder="https://raw.githubusercontent.com/..." required /></label></div><label>Exact source bytes<textarea placeholder="Paste the source that validators must evaluate" rows={12} required /></label><button className="primary-button" type="submit">Review constitutional fit ↗</button>{notice && <p className="notice">{notice}</p>}</form></section><section className="callout"><strong>Why this is gated</strong><p>A UI cannot safely invent a consensus result. The next implementation slice is the real proposal contract: it must evaluate C1–C4 through GenLayer consensus before the Governor can forward an upgrade.</p></section></Page>; }
